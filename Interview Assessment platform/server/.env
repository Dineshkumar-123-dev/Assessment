PORT=5000

# MongoDB connection URI (correct if MongoDB is running locally)
MONGO_URI=mongodb://127.0.0.1:27017/interview_platform

# JWT secret (should be a long, random, secret string)
JWT_SECRET=your_super_secret_jwt_key

# Email configuration (use real Gmail & app password or OAuth if needed)
EMAIL_USER=dineshkumarpmb2003@gmail.com
EMAIL_PASS=tjxnnncbfbhaklsw

# Frontend client (React app assumed on localhost)
CLIENT_URL=http://localhost:3000


origin: process.env.CLIENT_URL,



